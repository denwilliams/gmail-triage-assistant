# Google OAuth
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
GOOGLE_REDIRECT_URL=https://your-hostname.your-domain.com/auth/callback

# OpenAI
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4.1-nano

# Database
DATABASE_URL=postgres://gmail_triage:gmail_triage_password@db:5432/gmail_triage?sslmode=disable
# Used by Docker Compose to initialize the postgres service superuser password
POSTGRES_PASSWORD=changeme

# Session security — generate with: openssl rand -hex 32
SESSION_SECRET=replace-with-32-byte-random-key-in-production

# Server
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
